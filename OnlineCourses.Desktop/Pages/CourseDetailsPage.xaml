<Page x:Class="OnlineCourses.Desktop.Pages.CourseDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="CourseDetailsPage">
    <Grid Margin="24">
        <Border Padding="22"
                Background="White"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                CornerRadius="12">
            <StackPanel>
                <TextBlock FontSize="28"
                           FontWeight="SemiBold"
                           Text="{Binding Title}" />

                <TextBlock Margin="0,16,0,0"
                           FontSize="16"
                           Foreground="#4B5563"
                           Text="{Binding Description}"
                           TextWrapping="Wrap" />

                <WrapPanel Margin="0,18,0,0">
                    <Border Padding="10,4"
                            Margin="0,0,10,0"
                            Background="#EFF6FF"
                            CornerRadius="14">
                        <TextBlock Foreground="#1D4ED8"
                                   Text="{Binding Level}" />
                    </Border>

                    <Border Padding="10,4"
                            Background="#ECFDF5"
                            CornerRadius="14">
                        <TextBlock Foreground="#047857"
                                   Text="{Binding Price}" />
                    </Border>
                </WrapPanel>

                <TextBlock Margin="0,24,0,0"
                           Foreground="#6B7280"
                           Text="Здесь позже подключим реальные уроки и прогресс из API."
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
